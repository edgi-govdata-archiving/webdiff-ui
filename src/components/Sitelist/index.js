import React, {Component} from 'react';

class Sitelist extends Component {
  // Display a list of URLs for which there is an available change
  // Sort by most recently changed as a default?
  render() {
    return (
      <div>
        <p>This is a List of URLs that have diffs available to view</p>
      </div>
    );
  }
}

export default Sitelist;
